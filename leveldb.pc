prefix=/usr/local/leveldb
libdir=${prefix}
includedir=${prefix}/include
libname=leveldb

Name: leveldb
Description: leveldb v1.8 for github.com/brg-liuwei/golevel
Version: 1.8
Libs: -L${libdir} -l${libname}
Cflags: -I${includedir}

